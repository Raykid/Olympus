/**
 * 将数字变为指定长度的字符串，如果数字长度不够则在前方加0以填充长度，如果数字超长则使用原始长度
 *
 * @author Raykid
 * @date 2019-04-11
 * @export
 * @param {number} num 数字
 * @param {number} [len=1] 长度，默认是1
 * @returns {string}
 */
export declare function numToStr(num: number, len?: number): string;
